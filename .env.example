# Cloudflare API Credentials
# Get these from: https://dash.cloudflare.com/profile/api-tokens
CLOUDFLARE_API_TOKEN=your-api-token-here
CLOUDFLARE_ACCOUNT_ID=your-account-id-here
CLOUDFLARE_ZONE_ID=your-zone-id-here

# Tunnel Configuration
TUNNEL_NAME=my-service              # Must be unique across all your tunnels
DOMAIN=example.com                  # Your domain managed by Cloudflare
SUBDOMAIN=service                   # Leave empty for root domain
SERVICE_PORT=8080                   # Port your Docker service exposes

# Authentication Configuration
# Options: none, email, service_token
AUTH_METHOD=none

# For email auth (comma-separated list)
AUTH_EMAILS=user@example.com,admin@example.com

# Session duration for authenticated users (e.g., 24h, 7d, 30d)
SESSION_DURATION=24h

# Advanced Options (usually not needed)
# ORIGIN_REQUEST_CONFIG={"no_tls_verify": true, "connect_timeout": "30s"}